// settings.js — placeholder shell, filled in fully in a later step.
export function render() {
  return `
    <div class="page-header">
      <h1 class="page-header__title">Settings</h1>
    </div>
    <div class="page-content">
      <div class="empty-state">
        <div class="empty-state__icon">⚙️</div>
        <div class="empty-state__title">Settings coming next</div>
        <div class="empty-state__subtitle">Navigation is working — this section's real layout is built next.</div>
      </div>
    </div>
  `;
}

export function init() {}
